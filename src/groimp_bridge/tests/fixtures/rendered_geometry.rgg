module AxisProbe(float axisLength, float axisRadius) ==>
    Cylinder(axisLength, axisRadius);

module BallProbe(float ballRadius) ==>
    Sphere(ballRadius);

protected void init ()
[
    Axiom ==>
        AxisProbe(2, 0.5)
        [Translate(3, 0, 0) BallProbe(0.4)];
]
